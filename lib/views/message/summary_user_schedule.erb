直近<%= Settings.notice.cancel_day -%>日以内の予約で参加人数が<%= Settings.notice.cancel_member -%>人以下の日があります。キャンセル忘れに注意してください。
<% require_notice = false
   schedules.each do |schedule|
     break if schedule.start >= (Time.now + Settings.notice.cancel_day.day).beginning_of_day
     if schedule.count_ok <= Settings.notice.cancel_member -%>
-------------------
<%= schedule.date_ja(true) %>
<%= schedule.description %>
〇<%= schedule.count_ok -%> △<%= schedule.count_un -%> ×<%= schedule.count_ko %>
<%    end
    end
-%>
